//1. Crea una función que tome dos números y devuelva su suma.
function suma (a, b) {
  let total = a + b
  return total
}

console.log(suma(1, 2));

//2. Añade un escuchador de eventos a un botón que dispare la función cuando se haga click en él.